# Rafikipay - Docs

I moved top-level design/blueprint files into docs/ and cleaned the repository to keep runtime code in src/.

If you need a moved file restored as runtime code, tell me which one and I will convert it back to a .ts/.tsx module and update imports.
